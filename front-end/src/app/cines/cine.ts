export interface cineCreacionDTO{
    nombre: string;
    latitud: number;
    longitud: number;
}

export interface cineDTO{ //este va a ser un cine de lectura
    id: number;
    nombre: string;
    latitud: number;
    longitud: number;
}